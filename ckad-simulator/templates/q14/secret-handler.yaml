# Template: secret-handler Pod
# This file is copied to /opt/course/14/secret-handler.yaml during setup
# Q14: Student must add Secret env vars and volume mount
apiVersion: v1
kind: Pod
metadata:
  name: secret-handler
  namespace: moon
spec:
  containers:
  - name: secret-handler
    image: nginx:1.17.3-alpine
    ports:
    - containerPort: 80
